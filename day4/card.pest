File = _{ SOI ~ Card* ~ EOI }

Card = { CardHeader ~ CardNumbers ~ NEWLINE}

CardHeader = _{ "Card" ~ Number ~ ":" }

Numbers = { Number+ }

CardNumbers = _{ 
    Numbers ~ "|" ~ Numbers }

Number = @{ ASCII_DIGIT+ }

WHITESPACE = _{ " " | "\t" }